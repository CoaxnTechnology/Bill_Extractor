services:
  - type: web
    name: bill-extracter-backend
    runtime: image
    repo: https://github.com/CoaxnTechnology/Bill_Extractor.git
    plan: starter
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bill-extracter-db
          property: connectionString

databases:
  - name: bill-extracter-db
    plan: starter
    databaseName: bills_db
    user: bills_user
    ipAllowList: []
